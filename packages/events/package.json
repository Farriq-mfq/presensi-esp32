{
  "name": "@presensi/events",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev:events": "rimraf ./dist && tsup --watch",
    "build": "rimraf ./dist && tsup"
  },
  "devDependencies": {
    "@presensi/typescript-config": "workspace:*",
    "@types/node": "^20.10.1",
    "rimraf": "^5.0.5",
    "tsup": "^8.0.1"
  },
  "files": [
    "./dist/**"
  ],
  "author": "farriqmfq",
  "license": "ISC"
}
